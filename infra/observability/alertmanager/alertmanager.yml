global:
  resolve_timeout: 5m

route:
  receiver: discord-webhook
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 2h

receivers:
  - name: discord-webhook
    webhook_configs:
      - url: "http://host.docker.internal:5001/"
        send_resolved: true
